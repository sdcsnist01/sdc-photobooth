# Server-only (Vercel API routes). NO VITE_ prefix — never exposed to the browser.
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# App (used to build the QR code URL)
VITE_APP_BASE_URL=https://sdc-photobooth.vercel.app
